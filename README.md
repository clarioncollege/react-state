```bash
# Clone this project
$ git clone https://github.com/clarioncollege/react-state.git

# Access
$ cd react-state

# Install dependencies
$ npm install

# Run the project
$ npm run dev (Development server)

# visit <http://localhost:5173> on the browser to view
```
